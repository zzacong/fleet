# Command reference (moved)

This document has moved to the Starlight site at `apps/docs`.

- **New location:** [`apps/docs/src/content/docs/cli.md`](../apps/docs/src/content/docs/cli.md)
- **Site route:** `/cli/` when running `pnpm -F docs dev` or after `pnpm -F docs build` (see `apps/docs`)

The content is identical — the source of truth is now the Starlight collection. This stub remains so old links do not break.

