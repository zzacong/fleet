# 05: Drop docs

**What to build:** User-facing docs for `skill drop`. No config-key changes.

**Blocked by:** 04.

**Status:** resolved

- [x] `apps/docs/src/content/docs/cli.md`: `fleet skill drop` section (unlist vs delete, guards, adoptTarget rule, example output).
- [x] `README.md`: tracked-set paragraph mentions drop as the removal verb.
- [x] `CONTEXT.md` glossary: `Drop` entry if the vocabulary holds (one-time migration verb inverse to pull).
