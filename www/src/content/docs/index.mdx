---
title: Manage agent skills across AI coding agents
description: Manage agent skills across AI coding agents.
template: splash
hero:
  title: Fleet
  tagline: Manage agent skills across OpenCode, Codex, Claude Code, and more — one state file, every harness.
  actions:
    - text: Get started
      link: /installation/
      icon: right-arrow
    - text: Command Reference
      link: /cli/
      icon: right-arrow
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

![fleet demo — list skills, disable one for a harness, toggle in the matrix](/demo.gif)

## What Fleet does

Fleet keeps a single state file as the source of truth and projects it into each harness's own config. Skills never move, the canonical store (`~/.agents/skills`) is never edited, and the `skills` CLI stays the install and update backend.

<CardGrid stagger>
  <Card title="Turn off per harness" icon="setting">
    `fleet skill off tdd --harness pi` disables one skill in one harness —
    `fleet skill on` restores it.
  </Card>
  <Card title="Interactive matrix" icon="laptop">
    Bare `fleet` opens the skill × harness matrix: toggle, stage, and apply
    together.
  </Card>
  <Card title="Custom skills" icon="puzzle">
    `fleet skill adopt` moves a custom skill into your skills collection and
    wires it for every harness.
  </Card>
  <Card title="Doctor & Sync" icon="magnifier">
    `fleet skill doctor` previews drift; `fleet skill sync` converges configs
    with the state file.
  </Card>
</CardGrid>

## Supported harnesses

Fleet supports six harnesses: OpenCode, Pi, Codex, Claude Code, Cursor, and
Bob. See the [per-harness reference](/harnesses/) for files touched, written
shapes, and limitations.

## Documentation

- [Installation](/installation/) — script, npm, or Go
- [Command Reference](/cli/) — every verb, flag and error with examples
- [Per-Harness Reference](/harnesses/) — files touched, written shapes, limitations
- [State File Schema](/state-file/) — versioned, forward-compatible format
- [Undo & Escape Hatches](/undo/) — running `skills` by hand, resetting state, how sync decides
- [Skills Catalog](/skills/) — versioned collection in `skills/` at the repo root
