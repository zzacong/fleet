# Fleet Desktop

Reserved placeholder for the future Fleet GUI.

- **Path:** `apps/desktop`
- **Name:** `desktop` (locked; do not reuse this path for other apps)

No GUI code lives here yet. This directory reserves the name and path so the
monorepo layout stays stable. See ADR 0001 for the monorepo plan.
